server:
  address: '@product.host@'
  port: '@product.port@'
  servlet:
    context-path: /e-shop
spring:
  application:
    name: e-shop
  profiles:
    active: '@env@,@config-profile@,@messaging@,@message-queue-vendor@,@database-vendor@,@discovery-server-vendor@,@country@,@cache@'
