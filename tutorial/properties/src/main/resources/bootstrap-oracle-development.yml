spring:
  datasource:
    driver-class-name: oracle.jdbc.driver.OracleDriver
    url: jdbc:oracle:thin:@oracle.host@:eshop
    password: pine
    username: pine
    initialization-mode: never
  jpa:
    show-sql: true
    @oracle-orm@
