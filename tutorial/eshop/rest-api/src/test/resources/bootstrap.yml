server:
  address: localhost
  port: 0
  servlet:
    context-path: /eshop
spring:
  application:
    name: eshop
  profiles:
    active: test,properties,i18n,h2,hibernate,messaging,activemq-embedded
  main:
    banner-mode: off