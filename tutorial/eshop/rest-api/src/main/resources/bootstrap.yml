server:
  address: ${ESHOP_HOST:0.0.0.0}
  port: ${ESHOP_PORT:8080}
  servlet:
    context-path: /eshop
spring:
  main:
    banner-mode: off
  profiles:
    active: ${PROFILES}
  application:
    name: eshop