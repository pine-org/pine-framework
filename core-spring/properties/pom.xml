<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>core-spring</artifactId>
        <groupId>com.pineframework.core.spring</groupId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.pineframework.core.spring.properties</groupId>
    <artifactId>properties</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>jar</packaging>
    <name>Core Spring -> Properties</name>
    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>
    </properties>
    <build>
        <!--        <filters>-->
        <!--            <filter>src/main/resources/env/log/log4j-${env}.properties</filter>-->
        <!--            <filter>src/main/resources/env/mq/bootstrap-${message-queue-vendor}-${env}.yml</filter>-->
        <!--            <filter>src/main/resources/env/database/bootstrap-${database-vendor}-${env}.yml</filter>-->
        <!--        </filters>-->
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
                <includes>
                    <include>bootstrap-internationalization.yml</include>
                    <include>bootstrap-common-${env}.yml</include>
                    <include>bootstrap-${database-vendor}.yml</include>
                    <include>bootstrap-${message-queue-vendor}.yml</include>
                    <include>log4j.properties</include>
                    <include>banner.txt</include>
                </includes>
            </resource>
        </resources>
    </build>
</project>